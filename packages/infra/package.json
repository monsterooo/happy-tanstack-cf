{
	"name": "@happy-tanstack-cf/infra",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "alchemy dev",
		"deploy": "alchemy deploy",
		"release:pre": "alchemy deploy --stage pre --env-file .env.pre",
		"release:prod": "alchemy deploy --stage prod --env-file .env.prod",
		"destroy": "alchemy destroy"
	},
	"dependencies": {
		"dotenv": "catalog:",
		"zod": "catalog:"
	},
	"devDependencies": {
		"@happy-tanstack-cf/config": "workspace:*",
		"alchemy": "catalog:",
		"typescript": "catalog:"
	}
}
